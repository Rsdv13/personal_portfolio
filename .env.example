# Copy to .env for local development (gitignored). On Render, set these as real
# environment variables in the service's dashboard instead of committing a .env file.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
