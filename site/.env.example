# URL of the deployed Cloudflare Worker that proxies chat requests to OpenAI.
# Leave unset locally to run the site with the AI agent gracefully disabled.
# In CI (GitHub Actions), this is provided via the VITE_CHAT_API_URL repo secret.
VITE_CHAT_API_URL=https://sudharsan-agent.<your-cloudflare-subdomain>.workers.dev
